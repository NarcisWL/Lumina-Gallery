# Luvia Gallery v1.3.0（独立媒体播放器）

本次更新将媒体查看器重构为全局独立的播放器层：不再绑定浏览路径，任何视图都能以“当前列表为队列、所点媒体为起点”打开；`mediaId` 移出导航位置。

### 播放器架构
- 新增 `components/player/` 播放器层：队列状态机（纯函数 reducer）、`PlayerProvider` 全局上下文、`MediaPlayer` 查看壳与图片/视频面板。全库、文件夹、搜索、收藏视图点开图片或视频时，队列即该视图当前列表，可连续查看或播放；音频播放仍由独立的音频播放器承接。
- 播放器不再向浏览历史写入“正在看某张图”的条目：打开播放器不产生新历史记录，浏览器后退直接回到上一个浏览位置并收起播放器；同位置的关闭仍由 Esc、背景点击或关闭按钮完成。旧格式链接中的 `mediaId=` 参数被静默忽略，不再深链打开查看器。
- 查看器的“跳转到文件夹”按钮随重构移除（播放器已不绑定路径，定位语义由文件夹视图承接）；文件级重命名与删除入口此前仅存在于旧查看器，本次随组件移除而暂停，将在后续版本以新形态恢复；文件夹的重命名与删除不受影响。收藏、信息面板（EXIF）、全屏、幻灯片与键盘导航（Esc/←→）保持可用；播放器内收藏状态为打开时的快照，画廊层的心形实时状态不受影响。

### 移除与清理
- 删除旧 `ImageViewer` 组件与从未被引用的 `ViewerControls` 组件，以及查看器关闭时的历史回退逻辑（此前“跳转到文件夹”按钮失效的根因）。
- 修复播放器内信息面板跨开合残留：重新打开播放器时信息面板默认收起。

### 验证状态
- 新增播放器测试 12 例（队列状态机 4、Provider 2、播放器壳 4、视频面板 2）全部通过；全量前端测试 166/169，3 个失败为本机 Node 26 测试环境缺少 `localStorage` 全局的既有问题，与本次改动无关；Vite 生产构建通过，TypeScript 零新增错误。
- 视频编码探测与服务端转码为下一阶段独立计划；当前浏览器不支持的编码仍显示明确降级提示并提供下载。

# Luvia Gallery v1.2.8（图库顶部浮岛视觉）

本次更新对图库主区域做小幅视觉提升；毛玻璃浮岛与首行透明安全区修正均已于 2026-08-13 发布并部署到 FNOS 生产服务。

### 顶部浮岛视觉
- 桌面端图库导航从普通文档流改为悬浮覆盖，瀑布流延伸到导航背后，不再保留遮挡主区域的纯色占位块；移动端继续保留安全布局间距。
- 瀑布流滚动内容增加透明的桌面首屏安全区，第一行完整显示在浮岛下方；向下滚动后媒体仍可自然经过毛玻璃背后，不重新引入纯色遮挡块。
- 导航浮岛使用独立的半透明高斯模糊、饱和度和内高光材质，并为不支持背景模糊或启用“减少透明度”的环境提供高对比实色回退。

### 验证状态
- 前端测试 158/158 通过，Vite 生产构建与 `git diff --check` 通过。
- 精确提交 `2c352a6a0d02f088842ebac4a8c4f6934cba1770` 已构建为 `linux/amd64` 镜像；Docker Hub 的 `2c352a6-amd64` 与 `latest` OCI digest 均为 `sha256:6e317d1afd42dec40d20069944022d8c2681f8de5030b44b6471e018419c0d7f`。
- FNOS 候选容器与正式服务均通过首页、静态资源、内部 3001/3002 端口和真实管理员接口验证；正式容器零重启、未 OOM，扫描与缩略图任务均为空闲。
- 生产 Chrome 实测确认顶部工具栏为绝对定位，瀑布流延伸至浮岛背后；背景滤镜为 `blur(24px) saturate(1.55) contrast(1.04)`，控制台无 warning/error。
- 发布前数据库备份保存在 `.deploy/backups/2c352a6/lumina.db`，并保留 `rollback-2c352a6-pre` 镜像标签用于回滚。
- 首行透明安全区修正对应精确提交 `6cc0643736c52848b915e6892d65a55499778793`；Docker Hub 的 `6cc0643-amd64` 与 `latest` OCI digest 均为 `sha256:1a69d98e468bb3758e1201cee754870d98980f1d13f5a1e53fae014b7bda05d7`。
- FNOS 正式切换耗时 15 秒，容器零重启、未 OOM；908,014 条数据库副本 `quick_check=ok`，生产管理员首批 120 条、扫描和缩略图任务均正常。
- 生产 Chrome 在滚动顶部实测首张媒体顶部 96px、浮岛底部 82px，保留 14px 安全间距且没有重叠；向下滚动 100px 后媒体仍正常经过毛玻璃背后，控制台无 warning/error。
- 新版发布前备份位于 `.deploy/backups/6cc0643/lumina.db`，并保留 `rollback-6cc0643-pre` 镜像标签。

# Luvia Gallery v1.2.7（大媒体库首包与加载体验）

本次更新面向近百万媒体量级，优化媒体库首批查询、筛选分页和缩略图加载期间的视觉稳定性。该版本已于 2026-08-13 发布并部署到 FNOS 生产服务。

### 查询与分页
- WebUI 首批和续页从 500 条收敛为 120 条，图片、视频与音频筛选下推服务端，避免只过滤当前页。
- 扫描结果使用 `limit + 1` 判断续页；管理员无筛选全库读取增量统计精确总数，其它搜索、收藏、目录或授权作用域用 `totalExact` 区分已知下界与末页精确值。
- 普通列表仅对当前页判断收藏状态；收藏列表从用户的 ID 收藏集合驱动并按文件主键回表，移除 path OR、`DISTINCT` 和热路径精确全量计数。
- 新增默认日期、媒体类型、文件夹、全局/媒体类型/文件夹文件大小和用户收藏组合索引；旧数据库启动时幂等创建。
- 历史 path 收藏迁移在单一事务内先消除已存在 ID 收藏的冲突行，再归一化其余可映射记录；重复启动保持幂等。

### 加载与滚动体验
- 首次进入瀑布流显示约两个视口的比例骨架，续页在各列保留占位；网格首次和续页同样保留完整行骨架。
- 卡片优先使用服务端缩略图宽高比，缺失时按媒体 ID 生成稳定回退比例；图片加载后淡入，不再改变已分配高度。
- 首屏两行图片使用高优先级，其余图片异步懒加载；滚动采样使用 passive 监听和 `requestAnimationFrame`。
- 续页在约 1.5 个纵向视口前预取；网络失败或切换位置时分页锁会释放，可立即重试。
- 续页只有在当前数据集的媒体与目录首包完整成功后才开放；初始加载、失败或数据集切换窗口不会使用旧 offset 提前发起分页。
- 首包持久化缓存仅用于默认全库视图，并按用户名、角色和授权路径指纹隔离；搜索、筛选和排序结果不会覆盖首页缓存，媒体筛选也拥有独立查询缓存键。
- 受限账号进入页面时不再并发执行四次权限范围精确统计；系统设置以“—”显示非精确统计，避免用零值误导用户。
- 切换未缓存位置会清除旧目录项，首载骨架等待媒体与目录请求共同结束；搜索、收藏和全库空态不会覆盖仍在加载的骨架。

### 验证状态
- Node 20 后端测试 54/54、前端测试 157/157 通过，Vite 生产构建成功，`git diff --check` 通过。
- 百万行合成库中默认、视频、文件夹首批查询 p95 分别约 3.9ms、3.1ms、4.9ms；稀疏收藏经审阅后从约 388ms 优化到聚焦基准 p50 约 7.3ms、p95 约 39.7ms。合成数字用于相对回归，不代表生产绝对延迟。
- 本地 360 条合成媒体真实浏览器验证：24 张采样卡片加载期间累计布局偏移为 0，最大位置和高度变化均为 0。
- 精确提交 `e15dff19b579b8f7dd6eaa6ad5d152f77460c3b9` 已构建为 `linux/amd64` 镜像；Docker Hub 的 `e15dff1-amd64` 与 `latest` OCI digest 均为 `sha256:9ddb900a20cb75b4ed15eb143c709d4ab5c17103611738870867dc81b0bcdc84`。
- FNOS 在 908,014 条生产数据库副本上的首次索引升级耗时 40 秒，数据库由 1,372,475,392 字节增至 2,458,677,248 字节；7 个目标索引与 SQLite `quick_check` 均通过。正式切换耗时 58 秒，并保留一致性数据库备份与 `rollback-e15dff1-pre` 镜像标签。
- 生产真实管理员态验证中，日期首批、视频首批、大小首批与根目录接口分别为 14ms、11ms、25ms、13ms；首页、配置、PWA 清单、CSS/JS、3001 代理和 3002 应用均返回 200，容器零重启、未 OOM，扫描与缩略图队列均为空闲。
- 生产浏览器验证确认登录页 DOM 与视觉渲染完整，控制台无 warning/error；更新接口保护仍然有效。
- 瀑布流动态高度窗口化、随机排序和深页 cursor 迁移不在本轮范围，作为后续性能专项。
- 既有安全风险仍需另开专项：SQLite `LIKE` 的 ASCII 大小写语义可能扩大仅大小写不同的授权路径；配置中删除或改名管理员后，旧 JWT 在过期前仍可能保留令牌角色。本次部署没有扩大公网或多租户暴露面。

# Luvia Gallery macOS Widget (Remote Folder Browser)

本次更新将 macOS 悬浮相册“在线 → 文件夹”模式从手输路径升级为权限安全的可视化目录选择。

### 在线目录选择
- 填写服务器地址与 Wallpaper Token 后，可从当前账号的授权根目录逐层进入并选择实际目录。
- 每次只拉取当前层的直接子目录；返回已浏览层级时复用本次选择面板会话的缓存，不预取整棵目录树。
- 选择面板提供当前位置、返回上级、加载中、空目录、错误重试与“选择当前文件夹”状态。
- 虚拟授权根目录不可选择；文件夹模式未选择路径时禁止“立即加载”，避免意外退化为整个授权库随机加载。
- 目录浏览只调用 `GET /api/library/folders`，Token 仅通过 `Authorization: Bearer` 发送，不进入目录 URL 或目录请求日志。

### 工程质量
- 目录响应兼容服务端对象包装与旧边界顶层数组；未知或损坏结构失败关闭。
- 异步请求使用请求代次隔离，较慢的旧响应不能覆盖较新的导航状态。
- 本地目录来源继续使用系统目录选择面板，原有路径和轮播语义不变。

### 验证状态
- Swift 核心测试 28/28 通过，覆盖请求编码、响应兼容、导航缓存、旧响应隔离、取消、重复导航、空白 Token 和空路径加载门禁。
- Xcode Debug clean build 与 Release archive 成功；Release 为 arm64/x86_64 通用包，Apple Development 签名校验通过。
- 新包已安装到 `/Applications/LuviaGalleryWidget.app` 并从该路径成功启动，安装后二进制哈希与 Release 包一致。
- 运行时已读回“在线 → 文件夹”的只读路径和“浏览…”入口；本轮未再次发送已保存的 Token，真实鉴权目录列表留待用户主动点击验证。

# Luvia Gallery macOS Widget (Per-display Placement V2)

本次更新修复 MacBook 内置屏与外接显示器切换为主屏后，悬浮窗需要反复手动调整位置的问题。

### 窗口位置与多显示器
- 每台可可靠识别的物理显示器独立保存窗口尺寸及相对工作区位置，不再持久化易漂移的系统数字显示器 ID 和全局绝对坐标。
- 系统主屏改变后自动恢复新主屏档案；无关的屏幕参数通知不会把手动放在副屏的窗口拉回主屏。
- 分辨率、屏幕排列、Dock 与菜单栏引起的工作区变化会按当前可见区域重建并夹紧窗口。
- 显示器重配置期间暂停延迟保存和网格吸附；关闭按钮与菜单栏隐藏入口统一在隐藏前保存稳定位置。
- 无法可靠识别或同指纹冲突的显示器仅使用进程内档案，避免跨重启串屏。旧数字 ID 档案不自动迁移，升级后需在各屏首次放置一次。

### 验证状态
- Swift 核心测试 13/13 通过，覆盖坐标原点变化、工作区变化、物理指纹、冲突降级、主屏状态与非法工作区。
- Xcode Debug 构建与 Release archive 通过；Release 为 arm64/x86_64 通用包，Apple Development 签名校验通过。
- 新包已安装到 `/Applications/LuviaGalleryWidget.app` 并从该路径成功启动，当前外接屏已生成 `display-v2` 物理指纹档案；安装前版本同时保留为 zip 与废纸篓中的可恢复 `.app`。
- 真实内置屏/外接屏往返切换仍需在双屏连接状态下完成用户验收。

# Luvia Gallery v1.2.7 (Folder Listing Responsiveness)

本次更新修复超大目录进入子目录列表时可能长时间阻塞 Node.js 事件循环、导致 WebUI 持续加载或页面资源无法响应的问题。

### 性能与可观测性
- 目录封面选择改为显式使用 `idx_folder_path` 的精确路径与后代范围查询，避免 SQLite 误选媒体类型索引后对每个子目录重复扫描和排序。
- 同一目录列表请求会先批量取得封面结果，再组装响应；封面仍按最新修改时间选择，时间相同时按媒体 ID 稳定排序。
- 路径范围查询严格区分真实后代与同前缀兄弟目录，并保留尾分隔符路径与跨平台路径行为。
- 新增慢请求日志：超过 1 秒的请求仅记录方法、路由路径、状态码、耗时与完成方式，不记录查询参数、鉴权信息、Cookie 或请求正文。

### 验证状态
- Node 20 后端测试 36/36 通过，Vite 生产构建成功。
- 完整前端测试为 114/116；两个既有导航测试会在重复撤销全局存储桩后丢失 `localStorage`，与本次后端修复断言无关。
- 候选镜像使用生产数据库一致性副本和相同媒体只读挂载完成旁路验证：真实鉴权目录接口返回 200、235 个目录、耗时 122ms，并发健康请求全部为 200。
- 已部署至 FNOS 生产镜像 `sha256:982396a4bd5d483f85f8bf5facc55da2337dee3e36d93e727ec377211917958d`；生产同一目录接口耗时 116ms，并发健康请求最慢 110ms。
- FNOS 宿主、容器 3001/3002、外部地址及真实浏览器登录页均验证通过；生产容器重启次数为 0、未发生 OOM，启动日志无慢请求或应用错误。
- 部署前镜像保留为 `promenarleng/luvia-gallery:rollback-1dd3e59-pre`，可直接用于回滚。

# Luvia Gallery v1.2.6 (Media Hover Motion Preference)

本次更新为 WebUI 增加媒体缩略图悬浮缩放开关，便于希望减少界面动态效果的用户关闭放大动画。

### 设置与交互
- 在“设置 → 常规设置 → 外观设置”新增“媒体缩略图悬浮缩放”开关，并提供中英文说明。
- 开关默认开启以保持现有行为；关闭后，图片、视频和音频媒体卡片不再随光标悬浮放大。
- 文件夹封面、缩略图修复按钮、媒体信息浮层和视频悬浮预览保持原有交互，不受此设置影响。
- 偏好使用浏览器本地存储持久化，切换即时生效并在页面重载后恢复；存储不可用或值非法时安全回退为开启。

### 验证状态
- 媒体悬浮偏好与卡片样式定向测试 7/7 通过，Vite 生产构建成功。
- 已在隔离本地 WebUI 中验证开关的默认开启、即时关闭、视觉位置和重载持久化。
- 完整前端测试当前为 114/116；两个既有导航测试会在重复撤销全局存储桩后丢失 `localStorage`，与本次功能断言无关。

# Luvia Gallery v1.2.4 (Search Scope and API Safety)

本次更新统一媒体库、收藏夹和文件夹的搜索语义，并收紧搜索与扫描接口的分页参数校验。

### 搜索与浏览行为
- 媒体库搜索覆盖全库。
- 收藏夹仅展示直接收藏的文件，不递归展开收藏目录；收藏夹支持搜索、排序和媒体类型过滤。
- 文件夹仅在存在搜索词时递归搜索当前目录的全部后代；普通文件夹浏览行为保持不变。
- 在媒体库、收藏夹和文件夹等顶级视图之间切换时会清除旧的 `q`，避免上一视图的搜索词泄漏到新视图。
- 搜索无结果空态提供清除搜索入口。
- 普通文件夹卡片的计数逻辑未改动。

### API 与查询安全
- `scan/results` 的 `limit` 仅接受 `1..500` 的安全整数，`offset` 仅接受非负安全整数；非法参数返回 `400`。
- `query` 与 `count` 使用相同的搜索范围、路径、收藏和媒体类型条件，保证结果列表与总数一致。
- FTS 对普通文本搜索词进行安全处理，避免用户输入被直接解释为不受控的 FTS 查询语法。
- 修复路径前缀匹配边界，避免名称前缀相同但不属于目标目录后代的路径被误纳入结果。

### 验证与发布状态
- Node 20 后端测试 24/24、前端测试 107/107 通过，Vite 生产构建成功。
- 已通过候选容器旁路冒烟，并部署至 FNOS 生产；生产镜像为 `sha256:91cdaccb720b75dd03ae68f479a6c01b4f1650ad5a1e51f64593552cd5baa8f9`。

# Luvia Gallery v1.2.3 (Restorable Web Navigation)

本次更新重构 WebUI 导航与图库视口状态，使连续浏览目录和媒体时具备浏览器式历史控制，并能可靠返回离开前的位置。

### 导航与体验
- 将地址导航、作用域搜索、媒体筛选、排序和布局整合为单行浏览器式工具栏，删除无上下文内容时的常驻第二行。
- 媒体库、收藏夹和文件夹采用平行地址结构；非文件夹视图会在 URL、History 和位置键边界清除陈旧文件夹路径。
- 桌面端保留后退、前进、上一级与完整面包屑；1024px 以下使用紧凑工具栏，移动侧栏、搜索、筛选、排序和布局仍全部可达。
- 搜索使用当前空间作为明确作用域，提交结果在地址模式可见并可直接清除；排序、筛选和布局不新增历史节点。
- 搜索、筛选、排序、布局和移动端更多菜单的瞬时激活态支持点击工具栏外部或按 `Escape` 自动关闭；弹层互斥且不会清除已选设置。
- 统一导航栏采用独立且受控的层叠上下文，悬浮菜单稳定显示在媒体卡片之上，同时保持低于侧栏、图片查看器和系统模态层。
- 悬浮菜单使用主题感知的半透明实色背景与增强毛玻璃模糊，避免媒体图片透穿导致菜单文字难以辨认。
- 网格与瀑布流偏好按用户、图库和媒体库/收藏夹/文件夹作用域独立记忆；显式 URL 与浏览器 History 仍保持更高优先级。
- 暂时隐藏尚未具备百万级服务端时间索引能力的时间轴入口，并将旧时间轴状态安全回退为标准网格，避免选择状态与实际渲染不一致。
- 瀑布流卡片间距统一由布局容器控制，横向与纵向均为 16px，不再叠加额外底部留白。
- 浏览器 History 成为目录、收藏夹、搜索、排序和媒体查看器的统一历史事实源，保留既有 `#folder=` 深链兼容。
- 网格、时间线和瀑布流统一使用项目锚点与项目内偏移恢复位置，响应式列数变化时优先按稳定媒体 ID 定位。
- 打开图片或视频前同步固化当前视口，并按独立 History 条目恢复或回顶；关闭查看器后返回刚才的真实浏览位置，不再被延迟滚动采样覆盖为固定旧位置。
- 目录分页按用户、视图、路径、搜索、排序和随机种子隔离缓存；返回已浏览目录时复用已加载内容，超大目录的最新缓存不会自驱逐。
- 导航请求绑定位置世代、请求所有权和取消信号，旧目录请求不能覆盖新目录数据或遗留分页加载状态。
- 快照仅在显式导航恢复时发布，普通滚动捕获不会反向触发滚动，避免滚动发黏或跳回。

### 工程质量
- 引入 TanStack Query 管理 WebUI 服务端数据缓存，并增加 Vitest、Testing Library 与 jsdom 前端测试体系。
- 新增导航解析、History 控制器、查询键、三种视口锚点、导航控件及 App 导航竞态回归测试。

# Luvia Gallery v1.2.2 (Large Library Responsiveness)

本次更新针对超大媒体库在定时扫描期间出现的全站内容请求暂停进行性能优化，并加强扫描清理的数据安全门禁。

### 性能与稳定性
- 缓存统计和媒体扫描改用 `opendir` 异步流式遍历，文件状态读取限制为 16 路并发、每批最多 256 项，并在固定批次主动让出 Node.js 事件循环。
- 缓存统计与媒体扫描使用单一后台任务协调器，禁止两个约 90 万文件级任务重叠执行。
- 增量扫描不再一次性加载整个数据库；修改时间查询最多 512 条，清理使用 `rowid` 游标按 256 条分批执行。
- 任意目录读取、文件状态或数据库写入失败都会将本轮标记为不完整，并禁止删除索引和收藏，避免存储短暂离线时误清理。
- FTS、文件表和收藏删除纳入同一 SQLite 事务；任一写入失败会整体回滚并停止后续清理。
- 增加事件循环 p99/max 延迟监控，便于从生产日志直接识别主线程阻塞。
- 新增 Node 原生测试，覆盖流式让出、并发上限、扫描互斥、停止语义、游标清理和事务回滚。

# Luvia Gallery v1.2.1 (Production Stability)

本次更新针对 FNOS 生产环境中 `luvia-gallery` 容器内存持续膨胀与媒体浏览间歇卡顿进行稳定性修复。

### 修复与优化
- 修复 `runner.js` 代理层缺少连接生命周期清理的问题：客户端断开、上游异常、空闲超时都会主动销毁对应 socket，避免内部 `runner.js -> server.js` 连接长期滞留。
- 代理到内部后端时强制使用短连接并关闭 keep-alive 复用，降低图片/视频 Range 请求堆积导致的 socket 缓冲占用。
- 为 Supervisor HTTP 服务设置明确的 keep-alive、headers timeout 与代理空闲超时，提升异常网络场景下的鲁棒性。
- 为 Docker Compose 增加 `mem_limit`、`memswap_limit` 与 `NODE_OPTIONS=--max-old-space-size=2048`，防止容器无上限占用 FNOS 内存与 swap。

# 🚀 Luvia Gallery v1.2.0 (Kotlin Native Refactor)

本次更新完成了移动端从 React Native 到 Android 原生 Kotlin (Jetpack Compose) 的全面重构。

### 🆕 核心特性
- **Jetpack Compose UI**：100% 原生声明式 UI，带来更丝滑的交互体验与更低的内存占用。
- **类型安全导航**：全新的路由系统，彻底消除旧版中常见的导航状态异常。
- **动态服务器接入**：登录界面支持输入自定义服务器地址，完美支持私有化部署。
- **Stitch 设计系统**：完美复刻 Stitch 产出的高保真设计，提供极致的视觉质感。

### 🛠️ 架构升级
- **MVVM + Hilt**：标准化的原生开发模式，提升了代码的鲁棒性与可测试性。
- **Coil 图片加载**：优化的位图缓存管理，图库滚动帧率稳定在 120fps (支持高刷设备)。

# 🚀 Luvia Gallery v1.1.0

本次更新带来了搜索性能的质感飞跃以及移动端交互深度的全面进化。

### 🆕 核心特性
- **FTS5 搜索引擎**：服务端与移动端同步升级至 SQLite FTS5 原生全文检索，支持毫秒级模糊匹配。
- **层级文件夹跳转**：移动端现已支持点击“跳转到文件夹”后保留完整的父级目录回退栈，体验与 Web 端对齐。
- **极简 Header UI**：移除了所有页面冗余的副标题，优化主标题垂直居中对齐，界面更清爽。

### 🐞 修复与优化
- **分页加载修复 (GLM-5)**：彻底重构了图库与文件夹视图的 `onEndReached` 锁循环机制，解决无限重复加载导致的内存溢出。
- **安装包瘦身**：通过锁定 `arm64-v8a` 架构编译与清除多余本地库，包体积从 97MB 缩减至 **37MB**。
- **多端一致性**：同步了 Web 与 Mobile 的搜索占位符与交互反馈。

### 📦 构建信息
- **Android**: v1.1.0 (Release Build)
- **Architecture**: arm64-v8a
- **Signing**: Production Keystore Signed (promenar)
# Luvia Gallery v1.2.5 (Folder Search and Cover Reliability)

本次更新补齐文件夹搜索结果，修复部分目录封面长期空白，并统一 WebUI 的选中态与键盘焦点视觉。

### 搜索与目录封面
- 文件夹视图搜索同时返回匹配目录与递归媒体结果；目录结果来自 SQLite FTS5 文件索引，并限制为当前目录、当前用户授权路径和最多 100 条。
- 文件夹封面从数据库索引递归选择最新后代图片或视频，不再为每个目录同步递归遍历文件系统，避免超大媒体库下阻塞 Node.js 事件循环。
- 缩略图 Base64 ID 在进入 URL 路径前进行安全编码，避免 ID 中的 `/`、`+`、`=` 导致路由截断。
- 历史收藏目录重新校验当前用户权限；封面 URL 变化后，前端会清除旧的加载失败状态并重新尝试显示。
- 文件夹直接媒体计数补齐 GIF，并修复缺少扩展名前导点的匹配条件。

### 交互视觉
- 搜索作用域标签移除高对比白色描边，改用弱强调色背景。
- 按钮、菜单和列表的点击焦点不再保留白色外圈；键盘 Tab 导航继续使用仅 `focus-visible` 生效的强调色焦点环。
- 选中项统一使用强调色背景、文字和弱内描边；保留卡片、弹层和输入框用于结构分隔的低透明玻璃边框。

### 验证状态
- Node 20 后端测试 31/31、前端测试 110/110 通过，Vite 生产构建成功。
- 已通过隔离候选容器冒烟并部署至 FNOS 生产；生产 revision 为 `26d4cce`，镜像为 `sha256:48fb1256c53cb3a773deb5b7f3615f669678a5eec571b5f64c7371b29f88454e`。
- 生产首页、JS、CSS 与 manifest 均返回 HTTP 200；容器运行中、重启 0、OOM false，切换时内存约 86.9 MiB。
- 原生产镜像保留为 `promenarleng/luvia-gallery:rollback-26d4cce-pre`。

# Luvia Gallery v1.2.6 (Folder View Stability)

本次更新修复 WebUI 文件夹视图在连续跳转和图库扫描期间出现长时间空白或卡死的问题。

### 性能与可靠性
- 扫描状态改为读取 SQLite 增量统计快照，不再按轮询频率对大型媒体表执行四次全库计数。
- 文件夹封面改为缩略图就绪时写入派生缓存；文件夹列表只执行批量连接读取，不再为每个目录动态排序整棵后代树。
- 缺失或损坏的缓存缩略图不再下发封面 URL，避免目录页面触发现场 FFmpeg 风暴。
- 旧图库封面缓存通过后台协调器按游标分批恢复，批次间主动让出事件循环。
- WebUI 后台状态轮询改为单飞世代，目录和媒体请求增加 15 秒截止时间；加载失败时保留旧内容并提供重试入口。

### 验证与发布状态
- Node 20 全量后端测试 43/43、前端测试 121/121 通过，Vite 生产构建成功。
- 精确提交 `d36d66cd3075d532e31227dfa4c4f4e6cd4e70bd` 已构建并发布为 `linux/amd64` 镜像；Docker Hub 的 `d36d66c-amd64` 与 `latest` OCI digest 均为 `sha256:1d2910eba38d400fc36969d9e32dc5b55070c25b69593efeefe37efea69b467f`。
- FNOS 生产已使用 `--pull never` 切换到同一候选镜像；宿主 9980、容器内部 3001/3002、JS、CSS、manifest 和管理员状态/根目录接口均验证通过。
- 生产运行镜像 revision 为 `d36d66cd3075d532e31227dfa4c4f4e6cd4e70bd`，容器重启 0、OOM false；切换前镜像保留为 `rollback-d36d66c-pre`。
- 首次历史封面回填根据生产监控从 256 条批次进一步收紧为 32 条，并在批次间节流 10ms；回填从持久化游标继续执行。
